# Daily Progress Report — 2026-08-11

**Coverage window:** since the last daily report (2026-08-10 00:03 UTC) through Monday evening 2026-08-10 (~19:00 CDT). No commits are dated 2026-08-11 yet; this report captures the late-Sunday batch the previous report missed plus the full Monday workday.

---

### Pendulastic

* **Work Completed:**
  * **Three feature worktrees merged Monday morning:** `PatientIdentityTracker` wired into `batch_mediapipe.py` (`924fcfa`), Pick Person dialog added to the PostProcessingPanel upload flow (`99e574a`), and phone-camera wired as a recording source in AcquisitionPanel (`eb6cfa4`, closing out PR #8).
  * **IMU accuracy fix:** stopped knee-angle drift caused by an accel-bias unit/sign mismatch and a stray magnetometer correction (`aca4e89`) — touched the IMU server, calibration tuner, and app, with 200+ lines of new regression tests.
  * **New UI feature:** the acquisition panel now shows the IMU pairing address (`2a51d8f`, with tests).
  * **Two design cycles completed spec → review → implementation plan:** per-trace release-start alignment (1,393-line plan, `9f7562c`) and full-report HPE/IMU accuracy comparison (2,186-line plan, `f973c5e`). Both specs went through multiple Codex review passes before planning.
  * **Late-Sunday pipeline batch (7 commits):** bounded `synchronize_signals` cross-correlation lag search to ±5s, gated IMU zero-orientation capture on a confirmed trailing calm window, added raw-IMU replay fallback for `pt_score`'s imu_viewer curve, trimmed the sweep `WIDE_GRID`, fixed non-mirrored video path resolution, and widened the sweep cache fingerprint.
  * **Hygiene:** nightly audit ran (`6e7e190`) and 10 approved items were applied (`61c9ed3`) — `.gitignore` additions plus committing previously untracked status docs and plans.
  * Weekly Monday report drafted and simplified into plain-language sections (PR #15, draft).

* **Errors & Troubleshooting:**
  * **Knee-angle drift** traced to two compounding calibration bugs: accel bias applied with mismatched units/sign, and a magnetometer correction being applied where it shouldn't. Fixed in `pendulastic_imu_server.py` / `imu_calibration_tuner.py` and locked in with tests in `test_imu_server.py` and `test_batch_imu_vs_optitrack_rmse.py`.
  * **Clock-domain bug caught at design time:** a Codex review pass found the release-start alignment spec mixing clock domains; the spec was rewritten (`86c4e75`) before any code was written.
  * Stale Task 8/Task 9 cross-references in the release-start plan were corrected (`824311c`).
  * The nightly-hygiene runbook was failing when piped via stdin; now passed as a CLI arg in `run_nightly_hygiene.ps1` (`9246a46`).
  * Sweep cache was fingerprinting too few inputs, risking stale hits; fingerprint widened alongside the mirrored-video-path fix (`2d318cb`).

* **Key Learnings:**
  * Multi-pass external (Codex) review of specs keeps paying off — the clock-domain bug and round-3 trial-exclusion findings were all caught before implementation, where fixes are cheap.
  * Sensor-fusion corrections deserve explicit unit/sign audits; a single mis-signed bias term can masquerade as gradual drift.
  * Bounding the cross-correlation lag search window both cuts sync runtime and prevents pathological alignments.
  * Cache keys must fingerprint every input that affects output — the sweep cache bug was a reminder.

* **Backlog & Next Steps:**
  * Execute the two implementation plans now on main: release-start alignment, then full-report HPE/IMU accuracy comparison.
  * Seven draft report PRs are open and unmerged (#9–#15) — merge or close to clear the queue.
  * Several merged `worktree-*` and feature branches remain on origin — candidates for deletion via the next hygiene report.
  * Remaining `[Needs Review]` hygiene items from the 2026-08-10 report are still unapproved.

* **Process Improvements:**
  * Daily-report draft PRs are accumulating; consider auto-merging docs-only report PRs or folding them into a weekly rollup.
  * The spec → multi-pass review → plan → implement cadence worked well Monday — worth making it the default for any change touching the IMU/sync pipeline.
  * Worktree merges landed as local merge commits while PR #8 was closed rather than merged on GitHub; merging via the PR would keep GitHub history authoritative.

---

### solibra-web

* **Work Completed:** No activity in this window. Last commits were 2026-08-07 (compliance/design fixes: patient-journey diagram, investigational disclaimer, roadmap breakpoint gating).
* **Errors & Troubleshooting:** None — no sessions today.
* **Key Learnings:** N/A.
* **Backlog & Next Steps:** Draft PR #1 (Saturday progress report, week ending 2026-08-08) is still open awaiting review.
* **Process Improvements:** None to note for a quiet day.
