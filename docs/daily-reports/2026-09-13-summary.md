# Daily Progress Report — 2026-09-13 (Sunday; covering Saturday 2026-09-12 afternoon)

**TL;DR: Quiet Sunday — zero commits today in either repo. But Saturday afternoon (after the 09-12 report was written at 00:03) landed two CI-fix commits on Pendulastic's `feat/labview-bin-reader`, which is now 4 commits ahead of `main` with the export-contract job reproduced and fixed. solibra-web saw only its automated Saturday report (master now 32 days idle). Hygiene agent: still nothing newer than 08-31; next scheduled weeknight slot is Mon night (~1:17am Tue).**

### Pendulastic

* **Work Completed:**
  * **CI green-up for `feat/labview-bin-reader`** (Sat 13:16–13:24, after the 09-12 report cut-off):
    * `3f6b2c3` — export-contract job failed with `ModuleNotFoundError: No module named 'imu_calibration_tuner'`. Root cause: CI runs bare `pytest` (cwd not on `sys.path`) while local runs use `python -m pytest`; `tests/conftest.py` only bootstrapped `src/`. Fix bootstraps the repo root too. Also replaced the webapp job's exact-count discovery check (demanded exactly 108 tests; the suite has 392) with a floor (`MIN_TESTS=179`), carrying over `38ed646` which never reached `main`.
    * `e82b70e` — export-contract job was missing `pandas` and `matplotlib`, imported at module level via `imu_calibration_tuner → pendulastic_pt_score`. Reproduced in a fresh venv with exactly the job's packages, then verified 5 passed with the two added.
  * The branch now stands at 4 commits ahead of `main` (bin reader + events parser + `.gitattributes` + the two CI fixes), cleanly based on it.

* **Errors & Troubleshooting:**
  * Both CI failures were reproduced locally before fixing (bare `pytest.exe` for the sys.path issue; a fresh minimal venv for the missing deps) and re-verified passing after — no speculative pushes.
  * **Known-red, intentionally not fixed:** the webapp test asserting `build-id.js`'s `BUILD_ID` equals a fresh hash of SHELL and its files fails on `main` because `build-id.js` is stale; it is regenerated by `webapp/scripts/build-wasm.mjs` and belongs to the webapp rebuild routine, not this branch.

* **Key Learnings:**
  * `pytest` vs `python -m pytest` differ in whether cwd lands on `sys.path` — CI and local must bootstrap imports identically in `conftest.py` or they test different import graphs.
  * Exact test-count assertions in CI rot instantly (108 vs 392); a floor check catches discovery breakage without blocking growth.
  * Module-level imports (`pandas`, `matplotlib`) make transitive dependencies part of a job's install contract even when the test never uses them directly.

* **Backlog & Next Steps:**
  * Merge (or open a PR for) `feat/labview-bin-reader` — CI blockers on the branch are resolved; the stale `build-id.js` regeneration belongs to the webapp rebuild workflow.
  * Carryovers unchanged: **real-hardware validation of settle termination (critical path)**; 09-08 spec units A–D; hold-beep AudioContext device check; `webapp-static-deploy` PR + Vercel verification; full-corpus acceptance walk; full-dataset MediaPipe re-extraction; 5 stranded commits on `fix/app-teardown-imu-port-release`; `MIN_HINGE_CONDITIONING` derivation; report-PR backlog pass (drafts #19–#51+).

* **Process Improvements:**
  * The 00:03 report timing means same-day afternoon work slips a day — Saturday's CI fixes were invisible to the 09-12 report. Acceptable if each report explicitly covers "since the previous report" (as this one does); worth keeping that convention deliberate.
  * **Hygiene agent:** still nothing newer than `docs/reports/2026-08-31-hygiene.md`. Weekend slots aren't scheduled, so the streak stands at nine missed weeknights; if Tuesday ~1:17am produces nothing again, inspect the trigger directly or run it manually.

### solibra-web (Solibra Health)

* **Work Completed:** None by hand — only the automated CDO Saturday report (`be2efe4`, `docs/reports/2026-09-12-solibra-saturday-report.md`) on its own branch. `master` unchanged since `b798e8a` (2026-08-12), now 32 days idle.
* **Errors & Troubleshooting:** None — no sessions ran.
* **Key Learnings:** None today.
* **Backlog & Next Steps:** Unchanged: weekly Saturday report draft PRs still open — merge or prune. No feature work pending.
* **Process Improvements:** Nothing new.
