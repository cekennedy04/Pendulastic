# Daily Progress Report — 2026-08-05

## Overview

All of today's activity was in **Pendulastic** (20 commits, two workstreams, one merge to `main`). **solibra-web** had no commits or file changes today (last activity: July 28).

---

### Pendulastic

* **Work Completed:**
  * **IMU calibration track (Tasks 1–10, merged to `main` via `03a4a62`):** Landed Task 10 — stillness-gated accelerometer bias estimation and correction — wired into both the live path (`pendulastic_imu_server.py`) and the offline replay path (`imu_calibration_tuner.py`), following the proven gyro-bias pattern. Preceded by a one-off accel double-integration drift investigation (`analyze_accel_drift.py`), a new `batch_imu_vs_optitrack_rmse.py` for real IMU-vs-OptiTrack RMSE, and committed pre-fix vs post-fix comparison CSVs. Capped with an integration test suite (6 tests) and a deployment plan / monitoring strategy doc. Net diff for the day: ~2,327 insertions across 20 files.
  * **Phone-camera track (PR #8, still open):** Added a phone-camera dropdown entry and QR pairing panel to `AcquisitionPanel`, wired it into the App's camera-swap logic, and added a per-frame timestamp sidecar for phone-camera recordings.
  * **Recording UX:** Moved the webcam preview into a separate window with a large red countdown, removed the now-redundant embedded preview, and added a "Recording Saved" confirmation naming the exact folder/files written (scoped via a `from_recording` flag so upload-review paths don't claim a save that didn't happen).
  * **Reliability stats:** Extracted `reliability_stats.py` from the unrunnable `validate_controls.py`, added a per-family ICC reliability report to `evaluate_all_participants.py`, and strengthened its tests to verify actual RMSE/ICC values and blank handling.

* **Errors & Troubleshooting:**
  * **Blue-tinted live preview** (BGR/RGB channel-order mix-up) — fixed in the embedded RGB preview (`c9162b1`) and again in the phone-camera path along with empty phone-camera recordings (`67c23e6`).
  * **Webcam viewer window opening buried** behind the focused window (Windows focus-stealing prevention). `focus_force()` was tried first but hangs — it blocks on a window-manager focus event that never arrives without a live event loop. Fix: briefly toggle `-topmost` to pop the window to the front without permanently pinning it (`d6caaed`).
  * **`find_optitrack_match` cross-position collision risk** (flagged in code review): once the directory walk-up climbs above the trial's `Position_*` level, a same-numbered trial could silently match the wrong position's ground truth. Fix: new `_positions_with_trial()` guard that warns and skips when more than one position shares the trial number; verified byte-for-byte identical output against the real dataset for all 8 matched trials (`b70821b`).
  * **Full-suite regression check** after Task 10: 418/420 pass, 0 new regressions from the change.

* **Key Learnings:**
  * Accelerometer drift contributes meaningfully to RMSE — the diagnostic showed 0.05–34.58 m peak double-integrated displacement across real recordings — which motivated the Task 10 bias correction (estimate bias as mean raw accel minus gravity during verified-stationary windows, subtract before AHRS).
  * The gyro-gate fix alone left mean IMU-vs-OptiTrack RMSE essentially unchanged (~17.1°) — headline accuracy gains hinge on the accel correction, not the gating change.
  * Tkinter on Windows: `deiconify()` + `lift()` only affect stacking within the app; `focus_force()` can hang without an event loop pumping; a brief `-topmost` toggle is the reliable way to surface a window.
  * Path-walk matching heuristics need explicit scope guards when the data tree is shallow or inconsistent (the `Session_post`-level OptiTrack layout) — "works with today's data" isn't the same as safe.

* **Backlog & Next Steps:**
  * Review and merge **PR #8** (phone-camera wiring), then do an end-to-end test with a real phone pairing.
  * Run the full-participant regression comparison for the Task 10 accel-bias correction (`evaluate_all_participants.py` was re-enabled with `TARGET_PARTICIPANTS = []` for exactly this).
  * Execute the deployment plan / monitoring strategy documented in `5836ce7`.
  * Investigate the 2 outstanding test failures in the 418/420 run and confirm they're pre-existing.
  * Triage the accumulated `worktree-*` branches (pt-score-panel, raw-sensor-crosschecks, viewer-style, sequential-csv-intake, simplified-recording-folder-structure) — merge or delete.

* **Process Improvements:**
  * The same BGR/RGB tint bug was fixed twice in one day in two capture paths — centralize color conversion in a single shared capture/preview helper so the next camera source can't reintroduce it.
  * `worktree-*` branches are accumulating on the remote; adopt a merge-or-prune pass at the end of each work session.
  * Committing the RMSE comparison CSVs with the exact source commit noted (`86eea4e` pre-fix) was good provenance practice — worth making standard for all generated data artifacts.
  * Test counts reported today ranged from 11 to 97 to 418/420 depending on scope; a CI workflow (GitHub Actions) running the full suite on every push would make "did I regress anything" a non-question.

---

### solibra-web

* **Work Completed:** No commits or working-tree changes today; last activity was the initial site build on July 28. The stale remote `claude/pensive-cray-sckkfq` branch was pruned (it pointed at already-merged history).
* **Errors & Troubleshooting:** None — no activity.
* **Key Learnings:** None today.
* **Backlog & Next Steps:** Site build (brand, sections, assets) is committed to `master`; next milestone work (provider portal / clinical portal content) has not started.
* **Process Improvements:** None to note for an idle day.
