# Daily Progress Report — 2026-08-07 (workday Wed 2026-08-06 Central)

Covers all activity since the last report (2026-08-06 00:09 UTC / Tue 7:09 PM
Central). All of today's Pendulastic work — 50 commits, 67 files, ~22k
insertions on `claude/eager-ritchie-r5e56i` — is **already merged into `main`**.
Solibra Web had no activity today.

---

### Pendulastic

* **Work Completed:**
  * **Acquisition screen restyle** — restyled `AcquisitionPanel`, `ModeSelectView`, `UploadMetaView`, and `PostProcessingPanel` onto the shared palette; collapsed OptiTrack/Video File inputs behind a "Research sources" disclosure.
  * **Annotation features** — PT-score annotations drawn on the PostProcessingPanel angle plot (shared `draw_pt_annotations` extracted into `pendulastic_pt_score`), optional landmark collection in `run_offline_track`, and annotated video export with a codec-fallback helper (`camera_utils.open_video_writer`).
  * **Nightly code hygiene routine** — full spec→plan→implement cycle: `Finding` model, worktree-staleness classifier, untracked-file cruft classifier with hard data-directory exclusions, vulture dead-code parser with confidence-based whitelisting, markdown manifest renderer with stable item numbering, `run_hygiene_audit.py` CLI orchestrator, runbook, Task Scheduler wrapper script, and the approval convention documented in the root `CLAUDE.md`. First report generated (`docs/reports/2026-08-06-hygiene.md`) and items 15, 17, 29–70, 89, 96 applied.
  * **Clinical validation** — MAS-vs-PT-score validation + threshold-fitting pipeline (`mas_validation.py`, `fit_mas_thresholds.py`); standalone analysis/calibration scripts committed with tests.
  * **Design docs** — specs + implementation plans for Pick Person multi-patient disambiguation and the MS-vs-Control cohort comparison pipeline.

* **Errors & Troubleshooting:**
  * vulture's argparse rejected the whitelist path when placed after `--exclude` — fixed by reordering args; also switched to `python -m vulture` so it works without an activated venv.
  * Hygiene classifier bugs caught in review: branch-name extraction broke on slashed names (`feature/foo`), protected-directory checks only inspected the top-level path segment, and the main-branch worktree was misflagged `[Safe to Delete]` — all fixed.
  * Phase 1 of the audit only caught `CalledProcessError`; broadened to `ValueError`/`OSError`.
  * Live-mode panel appeared frozen on entry — fixed by painting the panel before the blocking camera rescan.
  * `load_hpe_model_curves` failed against the simplified recording folder structure — matching logic updated.
  * Unrelated in-progress work got swept into a commit; reverted out cleanly (`b17f6e4`).

* **Key Learnings:**
  * vulture has argparse ordering quirks and is more robust invoked as a module (`python -m vulture`) than via its console script.
  * Path-safety checks must test every path segment, not just the first — top-level-only checks silently miss nested protected dirs.
  * In the GUI, paint before any blocking I/O (camera rescan) or the UI reads as frozen.
  * The spec → implementation plan → build → review-wave loop caught real bugs (slashed branch names, worktree misclassification) before they could touch data.

* **Backlog & Next Steps:**
  * Verify the first unattended nightly hygiene run (~1:17 AM via Task Scheduler) and tune false positives.
  * Triage remaining 2026-08-06 hygiene-report items — `[Needs Review]` and `[Gitignore Candidate]` entries are still awaiting approval.
  * Implement Pick Person disambiguation and the MS-vs-Control cohort comparison pipeline (specs/plans done, no code yet).
  * Validate the fitted MAS thresholds against a larger clinical sample.
  * PR #8 (phone-camera dropdown + QR pairing) is still open for review/merge; draft report PRs #9 and #10 can be merged or closed.

* **Process Improvements:**
  * The one-day spec→plan→implement cadence worked well; keep the explicit review waves — they consistently surface real bugs.
  * A commit swept in unrelated work and had to be reverted — stage selectively (`git add -p`) or keep one worktree per feature.
  * Consider a small CI job (lint + the new test suite) so hygiene/classifier regressions are caught without a manual review wave.
  * (Carried from last report) The daily-report scheduler fires just after UTC midnight; naming now follows the UTC fire date, with the covered Central workday stated in the title to avoid the 08-06 collision.

---

### Solibra Web (solibra-web)

* **Work Completed:** No commits today; last activity was the initial site build on 2026-07-28.
* **Errors & Troubleshooting:** None (no sessions today).
* **Key Learnings:** None today.
* **Backlog & Next Steps:** Resume front-end work on the marketing site and provider portal when prioritized.
* **Process Improvements:** None to note for an idle day.
