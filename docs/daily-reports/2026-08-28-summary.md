# Daily Progress Report — 2026-08-28 (covers Thursday, 2026-08-27 workday)

**TL;DR: Three solid commits on `webapp-static-deploy` — the P24 full-report layout was fixed (table overflow, floating zone labels), and the first real Vercel deploy surfaced two hosting defects that are now fixed in the build itself (`vercel.json` emitted with no-cache pins; framework detection overridden).** But the branch has grown to **58 commits with no PR**, dwarfing the 5 still stranded on `fix/app-teardown-imu-port-release`. Hygiene agent stall reaches its 10th day. solibra-web idle (16 days).

### Pendulastic
* **Work Completed:**
  * `5662fc6` (13:59) — **fixed three layout defects in P24's full report** (`pt_report_common.py`, +360/−25 with 118 test lines). The row-5 source-agreement table clipped its headers because column widths were proportional to *character count*, not rendered width; cells now wrap into vertical slack and font size is shrunk-redrawn-measured iteratively. The PT-score panel's "Impaired" label floated above the axes when a band started above the visible range (P24's left leg tops out at 0.35, below the 0.44 floor); `visible_zone_bands()` now decides once for both spans and labels. Also: source-based coloring for RMSE bars and waveform overlays.
  * `f99e084` (14:22) — **build now emits `vercel.json` alongside `_headers`.** The dist bundle only carried `_headers` (a Netlify/Cloudflare convention Vercel ignores), so `sw.js` and `build-id.js` — the two files carrying the cache key — would be served with default caching and an installed phone would never learn a new build exists. Two new tests pin the no-cache/wasm guarantees and assert the two host configs agree on every shared path. 110 webapp tests pass.
  * `1abf50c` (15:17) — **fixed the failed first Vercel deploy** ("No python entrypoint found"): Vercel inferred Python from the surrounding repo and the dashboard preset outlived the upload. `vercel.json` now declares `framework: null`, no build/install command, output = the directory — the guess can't come back.
* **Errors & Troubleshooting:**
  * **Vercel deploy failure #1 (latent):** stale service worker caching — root-caused to `_headers` being a non-Vercel convention; fixed by emitting an equivalent `vercel.json` and testing the two configs against each other.
  * **Vercel deploy failure #2 (hard):** "No python entrypoint found" on a pure html/js/wasm upload — root-caused to framework auto-detection reading the repo (overwhelmingly `.py`) plus a sticky dashboard preset; fixed by explicit framework/build overrides in `vercel.json`.
  * **Matplotlib font-fitting no-op:** solving preferred/overflow ratio in one shot assumed rendered width is linear in point size — it isn't: 6.50pt and 6.19pt rasterize to the *same* 41px width. Fixed by iterating shrink → redraw → measure.
* **Key Learnings:**
  * **Host conventions are silent failure modes:** `_headers` deployed cleanly to Vercel and did nothing. Encoding both hosts' configs in the build and testing them for agreement turns a hosting choice into a checked invariant.
  * **Platform inference outlives its inputs:** Vercel's framework guess came from the repo, not the upload, and persisted in the dashboard. Explicit config beats correcting a preset.
  * **Measure, don't extrapolate, rendered text:** font metrics aren't linear in point size; a computed reduction can be a complete no-op.
* **Backlog & Next Steps:**
  * **Open a PR for `webapp-static-deploy` — now 58 commits off `main` with no PR**, including Wednesday's 14 durability/offline-shell commits (which yesterday's report also missed). This is now the largest unreviewable branch in the repo.
  * `fix/app-teardown-imu-port-release` still carries its 5 stranded post-merge commits (`56849fb`…`9a45d24`) — unchanged since Wednesday, still no PR.
  * **Verify the Vercel deploy actually went live** after `1abf50c`; the commit fixes the failure but no deploy success is recorded.
  * **Hygiene agent: 10th day stalled** (nothing in `docs/reports/` newer than 2026-08-18) — needs a manual look at the schedule config.
  * Report-PR stack is now **17 open drafts (#19–#35)** — batch-merge or prune.
  * Carried over: unexplained ~13 s exit delay; m/s² ground-truth trial for the stillness gate; calibration-hold protocol decision; validation-plan branch untouched.
* **Process Improvements:**
  * **The stranded-branch anti-pattern has scaled up, not down:** flagged two reports running for a 5-commit branch, it now has a 58-commit sibling. Suggestion stands and is more urgent: open the (draft) PR in the same session that pushes the first commit to a branch.
  * Wednesday's 14 webapp commits escaped yesterday's report — likely pushed after the 00:04 UTC run. Worth pushing branches before end of day so the nightly report sees them.
  * The Vercel work modeled the right response to environment failures: fix them in the *build output* with tests, not in the dashboard, so the fix survives redeploys and host switches.

### solibra-web (Solibra Health)
* **Work Completed:** None — no commits or PR activity. `master` unchanged since `b798e8a` (2026-08-12), 16 days idle.
* **Errors & Troubleshooting:** None — no sessions ran.
* **Key Learnings:** None today.
* **Backlog & Next Steps:** Unchanged: draft report PRs #2 and #3 still open — merge or prune. No feature work pending.
* **Process Improvements:** Nothing new.
