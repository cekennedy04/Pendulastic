# Daily Progress Report — 2026-08-05 (continued)

**Note:** This report fired at 00:07 UTC on 2026-08-06 — only 7 minutes into the UTC calendar day, but still Wednesday evening (7:07 PM Central) for the actual work session. The bulk of today's Pendulastic work was already captured in the previous report (PR #9, generated 14:25 Central). This entry covers the one commit made *after* that report ran.

### Pendulastic
* **Work Completed:**
  * `f2f2cd3` (15:04 Central) — **Countdown/viewer window now shows for any recording, not just RGB.** Previously the big red countdown/viewer window only opened via the RGB camera's live/lost callback, so a phone-IMU-only trial (no webcam checked) never got it — the exact "operator has stepped back" visibility problem the window exists to solve, just without a camera in the mix. Replaced the single camera-live trigger with `_sync_viewer_window_visibility()`, which shows the window on the OR of three independent conditions: camera live, countdown active, or recording active. Wired into `set_camera_live()`, `_start_countdown()`, `_cancel_countdown()`, `enter_recording()`, and `enter_idle()`. Added 3 new tests covering IMU-only countdown, countdown-cancel hiding, and camera-drop-mid-countdown (window must stay up).
* **Errors & Troubleshooting:** None new — this commit closed a visibility gap rather than fixing a bug.
* **Key Learnings:** Visibility state for UI elements driven by multiple independent triggers (camera state, countdown state, recording state) is more reliable as a single OR'd sync function called from every state-transition site, rather than one-off shows/hides scattered per callback — easy to miss a trigger otherwise (as the IMU-only case was missed initially).
* **Backlog & Next Steps:** Unchanged from PR #9 — PR #8 (phone-camera wiring) still open for review/merge; Task 10 accel-bias full-participant regression run still outstanding; `worktree-*` branches still need a merge-or-prune pass.
* **Process Improvements:** The scheduled daily-report task uses a UTC calendar-day boundary to define "today," but development happens in US Central time (UTC-5). A report firing shortly after UTC midnight can miss hours of same-workday activity (as happened here) or double-report it. Fix: base the "today" window on the last report's timestamp (or Central-time midnight) rather than a literal UTC date match.

### solibra-web
* **Work Completed:** None — unchanged since the initial site build on 2026-07-28.
