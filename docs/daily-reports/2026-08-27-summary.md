# Daily Progress Report — 2026-08-27 (covers Wednesday, 2026-08-26 workday)

**TL;DR: A quiet, focused day — two startup-performance commits on `fix/app-teardown-imu-port-release`, cutting app module import from ~60 s to ~4.4 s by deferring mediapipe and scikit-learn.** An unreachable scoring safeguard was also removed after verification against all 28 archived knee-angle traces. **The stranded-commit problem flagged yesterday got worse, not better: that branch now carries five post-merge commits with no PR.** Hygiene agent still stalled (newest report remains 2026-08-18). solibra-web stayed idle.

### Pendulastic
* **Work Completed:**
  * `0eeb6ab` (Tue 23:47) — **stopped importing mediapipe at startup.** Profiling the 21 s window-open delay showed `import mediapipe` cost 15.7 s in-process; an A/B of the app module import measured **59.78 s eager vs 17.95 s deferred** (~21 s vs ~2 s on a quiet machine). `pendulastic_viewer` imported it purely to probe availability (now a cached `_mp_available()`), and `pendulastic_app`'s module-level handles became `_ensure_mediapipe()`. New tests assert mediapipe is absent from `sys.modules` after import, checked in a subprocess.
  * `9a45d24` (Wed 11:43) — **deferred scikit-learn/scipy.stats in `mas_validation`** (only two functions need them; the app was loading sklearn to populate a dropdown): app import 5.86 s → 4.42 s. Also **removed `_merge_close_extrema()`, which never ran** — `find_peaks(distance=fps/3.5)` already guarantees wider separation than the fps/6 merge threshold. Verified over all 28 archived knee-angle traces: every parameter identical to nine decimal places; a new test pins the separation relationship so future tuning fails loudly.
* **Errors & Troubleshooting:**
  * The first version of the `_ensure_mediapipe()` guard checked the wrong module handle and **silently returned no poses** — caught by `test_biomechanical_engine`, which injects a fake mediapipe module; the guard now returns early when `_mp` is already set so test injection is honoured.
  * Profiling left one mystery open: **~13 s between the window disappearing and process exit** is unexplained (mediapipe teardown is only 1.8 s of it).
* **Key Learnings:**
  * **Dead code survives by looking like a safeguard.** `_merge_close_extrema()` sat untouched because it read as active protection against double-counted spastic catches — no test covered it, and its inputs were already filtered by `find_peaks`'s distance guarantee. Whether the safeguard *should* exist is a separate scoring/validation question, recorded in the commit.
  * **Test the invariant, not the stopwatch:** asserting `sklearn`/`mediapipe` absent from `sys.modules` in a subprocess is deterministic where a wall-clock threshold would flake on shared machines.
  * Remaining startup cost is mapped: `pendulastic_pt_score`'s scipy/pandas surface is 26 call sites through core scoring math — correctly sized as its own task rather than scattered edits.
* **Backlog & Next Steps:**
  * **Open a PR for `fix/app-teardown-imu-port-release` — now five stranded commits** (`56849fb`…`9a45d24`), not on `main`, invisible to review. This was yesterday's top priority and is now two commits more urgent.
  * Chase the unexplained ~13 s exit delay; decide whether the remaining ~4.4 s import (scipy.signal 2.48 s + cv2 1.25 s) is worth the pt_score refactor.
  * Carried over: m/s² ground-truth trial for the loosened stillness gate; ~10 s calibration-hold protocol decision; validation-plan branch (`claude/mobile-app-validation-plan-u94fbs`) still untouched.
  * **Hygiene agent stall enters its ninth day** — `docs/reports/` still has nothing newer than 2026-08-18; the schedule config needs a manual look.
  * Report-PR stack (#28–#34 plus this one) still needs a batch merge/prune.
* **Process Improvements:**
  * **The flagged anti-pattern compounded:** yesterday's report warned against pushing follow-ups to a merged PR branch; two more landed there anyway. Make "open the PR" part of the same session that pushes the first post-merge commit — the branch work is excellent and currently unreviewable.
  * The perf work modeled good practice worth repeating: profile first, A/B with alternation to average machine load, and record what was *not* addressed (the 13 s exit gap) so it isn't mistaken for solved.

### solibra-web (Solibra Health)
* **Work Completed:** None — no commits or PR activity. `master` unchanged since `b798e8a` (2026-08-12).
* **Errors & Troubleshooting:** None — no sessions ran.
* **Key Learnings:** None today.
* **Backlog & Next Steps:** Unchanged: weekly `claude/solibra-report-*` branches (08-08/08-15/08-22) sit on origin; #2 and #3 are still open draft PRs — merge or prune. No feature work pending.
* **Process Improvements:** Nothing new.
