# Hygiene Report - 2026-08-31

1. `[Needs Review]` 'excluded_trials.json.bak-2026-08-28-pre-P17-pre-exclusion' is untracked with no known pattern match - needs a human look.
```bash
git status -- excluded_trials.json.bak-2026-08-28-pre-P17-pre-exclusion  # decide: add, gitignore, or delete
```

2. `[Needs Review]` models\HigherHRNet-Human-Pose-Estimation-master\lib\fp16_utils\fp16_optimizer.py:506: unreachable code after 'raise' (100% confidence)
```bash
# review models\HigherHRNet-Human-Pose-Estimation-master\lib\fp16_utils\fp16_optimizer.py:506 before removing
```

3. `[Needs Review]` models\pose2sim-main\Pose2Sim\poseEstimation.py:791: unused import 'as_completed' (90% confidence)
```bash
# review models\pose2sim-main\Pose2Sim\poseEstimation.py:791 before removing
```

4. `[Needs Review]` models\PosePipeline\pose_pipeline\wrappers\fairmot.py:123: redundant if-condition (100% confidence)
```bash
# review models\PosePipeline\pose_pipeline\wrappers\fairmot.py:123 before removing
```

5. `[Needs Review]` models\PosePipeline\PosePipeline-main\pose_pipeline\wrappers\fairmot.py:123: redundant if-condition (100% confidence)
```bash
# review models\PosePipeline\PosePipeline-main\pose_pipeline\wrappers\fairmot.py:123 before removing
```

6. `[Needs Review]` models\SKEL-master\skel\utils.py:208: unreachable code after 'return' (100% confidence)
```bash
# review models\SKEL-master\skel\utils.py:208 before removing
```

7. `[Needs Review]` train_angle_regressor.py:427: unused variable 'median_shank_px' (100% confidence)
```bash
# review train_angle_regressor.py:427 before removing
```

8. `[Doc Drift Fix]` `README.md`'s Quick Start cannot run against the installed environment. `src/pendulastic/pose.py:12` does `_POSE = mp.solutions.pose`, but the venv has mediapipe 0.10.35, which exposes only the Tasks API and has no `solutions` attribute. The Quick Start's second line (`from pendulastic.pose import extract_landmarks`) therefore raises `AttributeError` at import time, so the documented five-line usage example fails before it reaches any of the project's own logic. `tests/test_pose.py` and `tests/test_metrics.py` error out at collection for the same reason. Note the README's Key Features section ("tracks hip, knee, and ankle landmarks using MediaPipe") describes this same dead path, while the pipeline that actually runs (`mediapipe_worker.py`) is a separate, working Tasks-based implementation.
```bash
.venv/Scripts/python.exe -c "import sys; sys.path.insert(0,'src'); import pendulastic.pose"  # reproduce the AttributeError
```

9. `[Doc Drift Fix]` `README.md`'s License section states "This project is licensed under the MIT License. See [LICENSE](LICENSE) for details." No `LICENSE` file exists anywhere in the repo, so the link is dead and the project ships with no license text at all.
```bash
ls LICENSE*  # confirm: no such file
```

10. `[Doc Drift Fix]` `README.md`'s Dependencies section says "See [requirements.txt](requirements.txt) for pinned versions." `requirements.txt` contains no pins - every entry is a `>=` floor (and `statsmodels` has no constraint at all). This is the mechanism behind item 8: `mediapipe>=0.10.14` resolved forward to 0.10.35, which removed the `mp.solutions` API `src/pendulastic/pose.py` depends on.
```bash
cat requirements.txt  # all >= floors, zero == pins
```

11. `[Doc Drift Fix]` `DEPLOYMENT_PLAN.md`, `ALGORITHM_LAUNCH_CONFIRMATION.md`, and `IMPACT_MEASUREMENT_REPORT.md` all assert that deployment monitoring is live - "Monitoring Active", "Dashboards deployed and accessible", "Alerts configured and operational", "RMSE regression >10% -> Alert", "Application stability monitoring", "uptime >99.9%", "Results arriving in real-time dashboard". No such infrastructure exists in the repo: there is no alerting code, no uptime or crash monitoring, and no RMSE-regression watcher. The only file matching "dashboard" is `longitudinal_dashboard.py`, which renders a 3-panel per-participant clinical figure from `pendulastic_storage.load_history()` and has nothing to do with deployment monitoring. These are the load-bearing claims under each doc's "how we will know it worked" section, which is why items 14 and 15 below were never closed out.
```bash
ls | grep -i "monitor\|alert\|dashboard"  # only longitudinal_dashboard.py, a clinical figure renderer
```

12. `[Doc Drift Fix]` The "418/420 tests passing" figure is quoted as current evidence across `DEPLOYMENT_PLAN.md`, `GOAL_ACHIEVEMENT_ASSESSMENT.md`, `ALGORITHM_LAUNCH_CONFIRMATION.md`, and `IMPACT_MEASUREMENT_REPORT.md` - including "Confirm 418/420 tests still pass" as a live action item. `tests/` now collects **1494** tests (the suite has more than tripled since 2026-08-05), and 2 files (`tests/test_pose.py`, `tests/test_metrics.py`) fail at collection outright per item 8. The 420 denominator no longer refers to anything that exists, so "418/420, 0 regressions" can neither be verified nor refuted as written. Related: item 33 of the 2026-08-18 report (`integration_test_task10.py` failing, cited in these same docs as "6/6 PASS") was re-run today and **still fails** at collection - `accel_bias` comes out `[0.0019, -0.00095, 0.1906]` vs the test's hardcoded `[0.1, -0.05, 0.19]`.
```bash
.venv/Scripts/python.exe -m pytest tests/ --collect-only -q | tail -5  # 1494 collected, 2 errors
```

13. `[Doc Drift Fix]` `DEPLOYMENT_PLAN.md` claims CI runs the Python test suite: "Verify CI/CD pipeline passes" (Phase 1), "Run full test suite daily (automated via CI/CD)" (Phase 3), "CI/CD pipeline passes all tests" (Success Criteria). `.github/workflows/ci.yml` has three jobs - `rust` (cargo test/clippy/build for `mobile-imu-core`), `export-contract` (`pytest tests/test_web_export_contract.py -q`, one file), and `webapp` (node tests, asserting exactly 108). Nothing runs `tests/`, nothing runs on a daily schedule (`on: [push, pull_request]` only), and no job exercises the IMU accel-bias code these docs are about.
```bash
cat .github/workflows/ci.yml  # three jobs; one Python test file, no schedule
```

14. `[Doc Drift Fix]` **Repeat of item 34 in the 2026-08-18 report - still unfixed 13 days later, and the gap has widened.** `GOAL_ACHIEVEMENT_ASSESSMENT.md` still says "Baseline RMSE: Unknown (model predictions not available in test environment)", "Goal Status: ON TRACK (70% Complete)", and "Probability of Meeting <5 Degree Goal: MODERATE (40-70%)". The baseline is no longer unknown. `docs/reports/2026-08-17-imu-methodology-comparison.md` measured it against OptiTrack across 53 real trials: mean **16.8 deg**, median 14.0 deg, 1/53 under the 5 deg goal - and after the grid-searched config was persisted, mean **14.843 deg**, median 10.977 deg, 2/53 under goal. The doc's own probability table puts a >10 deg baseline at "Very Low (<5%)" chance of reaching the goal, contradicting its own headline. Separately, `docs/reports/2026-08-28-imu-over-read-root-cause.md` finds the dominant residual error is single-sensor geometry (the thigh sweeps a median 16.7 deg), "NOT fixed, and not fixable in analysis" - i.e. not reachable by any of the accel-bias refinements in the doc's "Medium-term (If Gap Remains)" list.
```bash
grep -n "Baseline RMSE\|ON TRACK\|MODERATE (40-70%)" GOAL_ACHIEVEMENT_ASSESSMENT.md  # vs docs/reports/2026-08-17-imu-methodology-comparison.md
```

15. `[Doc Drift Fix]` `IMPACT_MEASUREMENT_REPORT.md` is titled "RMSE Impact Measurement Report" and marked "COMPLETE", but measures no impact: every RMSE number in it is a projection ("Expected improvement: 3-15%", "Confidence level: HIGH") derived from the Task 5 drift diagnostic, not from any before/after RMSE comparison. Its "Measurement Period" is "Deployment + 1 hour validation window" and it sets "**Next Review:** After 1 week of production monitoring" - now 26 days overdue. The measurement it was waiting for has since happened (see item 14) and was never folded back in. The report's premise also did not survive: it predicts "3-20% average" improvement for the Pendulastic IMU family from accel-bias correction, while the 2026-08-28 root-cause investigation attributes the real error to a drift-correction defect in the *scoring* code (fixed; 54.9% -> 31.8% median error) plus single-sensor geometry - neither of which is accel bias.
```bash
grep -n "Next Review\|Measurement Period\|Expected improvement" IMPACT_MEASUREMENT_REPORT.md  # projections, not measurements
```

16. `[Doc Drift Fix]` `DEPLOYMENT_PLAN.md`'s "Files Changed Summary" cites stale line counts - `analyze_accel_drift.py (156 lines)` is 161, `tests/test_analyze_accel_drift.py (42 lines)` is 48 (`integration_test_task10.py (113 lines)` is correct). More substantively, in a document whose status banner reads "DEPLOYED - merged to `main` as commit `03a4a62` on 2026-08-05", every box under "Post-Deployment Validation" is still unchecked 26 days on (Day 1, Week 1, and Month 1 checklists - 15 items), and all four "Deployment Team" roles still read "[To be assigned]". Either the validation happened and the doc was never updated, or it did not happen and the deployment was never validated; the doc as written cannot distinguish the two. (Verified separately: the code claims in `ALGORITHM_LAUNCH_CONFIRMATION.md` all hold - `_IMUDevice.accel_bias`, `on_accel()`, `calibrate_accel_bias()`, `zero()` wiring, `_RoleState.accel_bias`, `replay_trial()` correction, and `GYRO_BIAS_WINDOW_S = 1.0` are all present as described. The drift is in the validation and monitoring claims, not the algorithm description.)
```bash
wc -l analyze_accel_drift.py tests/test_analyze_accel_drift.py integration_test_task10.py  # 161 / 48 / 113
```

## Low-confidence dead-code findings (not numbered - review only)

- camera_utils.py:143: unused method 'rescan' (60% confidence)
- data_purpose.py:74: unused variable 'ACCEPTABLE_WITHOUT_IMU' (60% confidence)
- data_purpose.py:192: unused function 'imu_is_results_grade' (60% confidence)
- evaluate_live_vs_replay_calibration.py:33: unused variable 'WINNING_PARAMS' (60% confidence)
- generate_group_condition_figures.py:35: unused variable 'AQUA' (60% confidence)
- generate_longitudinal_and_score_figures.py:127: unused function 'mas_for' (60% confidence)
- generate_longitudinal_and_score_figures.py:350: unused variable 'col_i' (60% confidence)
- generate_pt_score_critique.py:116: unused variable 'u_stat' (60% confidence)
- models\openpose\3rdparty\pybind11\tests\test_tagbased_polymorphic.py:22: unused attribute 'sound' (60% confidence)
- models\PosePipeline\pose_pipeline\utils\jupyter.py:4: unused function 'play' (60% confidence)
- models\PosePipeline\PosePipeline-main\pose_pipeline\utils\jupyter.py:4: unused function 'play' (60% confidence)
- pendulastic_pt_score.py:1654: unused function '_dominant_frequency_hz' (60% confidence)
- pendulastic_pt_score.py:2545: unused variable 'model_peak' (60% confidence)
- pendulastic_pt_score.py:2568: unused variable 'model_peak' (60% confidence)
- tests\conftest.py:87: unused function 'pytest_unconfigure' (60% confidence)
- tests\test_imu_calibration_tuner.py:358: unused variable 't_contam' (60% confidence)
- tests\test_pendulastic_storage.py:12: unused function '_isolated_participants_dir' (60% confidence)
- tests\test_pendulastic_workbench.py:14: unused function '_isolated_participants_dir' (60% confidence)
- tests\test_trial_quality_tags.py:9: unused function '_isolated_registries' (60% confidence)
- tests\test_web_export_contract.py:126: unused variable 'ang_good' (60% confidence)
- train_angle_regressor.py:564: unused method 'on_epoch_end' (60% confidence)
- train_angle_regressor.py:640: unused method 'on_epoch_end' (60% confidence)

=== NEXT_ITEM_NUMBER: 17 ===
