# Hygiene Report - 2026-08-10

1. `[Safe to Delete]` Worktree 'C:/Users/cladi/Pendulastic/.claude/worktrees/acquisition-clinician-ux' (branch worktree-acquisition-clinician-ux) is fully merged into main.
```bash
git worktree remove C:/Users/cladi/Pendulastic/.claude/worktrees/acquisition-clinician-ux && git branch -d worktree-acquisition-clinician-ux
```

2. `[Safe to Delete]` Worktree 'C:/Users/cladi/Pendulastic/.claude/worktrees/imu-auto-tare' (branch worktree-imu-auto-tare) is fully merged into main.
```bash
git worktree remove C:/Users/cladi/Pendulastic/.claude/worktrees/imu-auto-tare && git branch -d worktree-imu-auto-tare
```

3. `[Safe to Delete]` Worktree 'C:/Users/cladi/Pendulastic/.claude/worktrees/pendulastic-video-hpe-fixes' (branch worktree-pendulastic-video-hpe-fixes) is fully merged into main.
```bash
git worktree remove C:/Users/cladi/Pendulastic/.claude/worktrees/pendulastic-video-hpe-fixes && git branch -d worktree-pendulastic-video-hpe-fixes
```

4. `[Safe to Delete]` Worktree 'C:/Users/cladi/Pendulastic/.claude/worktrees/sequential-csv-intake' (branch worktree-sequential-csv-intake) is fully merged into main.
```bash
git worktree remove C:/Users/cladi/Pendulastic/.claude/worktrees/sequential-csv-intake && git branch -d worktree-sequential-csv-intake
```

5. `[Safe to Delete]` Worktree 'C:/Users/cladi/Pendulastic/.claude/worktrees/simplified-recording-folder-structure' (branch worktree-simplified-recording-folder-structure) is fully merged into main.
```bash
git worktree remove C:/Users/cladi/Pendulastic/.claude/worktrees/simplified-recording-folder-structure && git branch -d worktree-simplified-recording-folder-structure
```

6. `[Safe to Delete]` Worktree 'C:/Users/cladi/Pendulastic/.claude/worktrees/waveform-t0-alignment' (branch worktree-waveform-t0-alignment) is fully merged into main.
```bash
git worktree remove C:/Users/cladi/Pendulastic/.claude/worktrees/waveform-t0-alignment && git branch -d worktree-waveform-t0-alignment
```

7. `[Safe to Delete]` Worktree 'C:/Users/cladi/Pendulastic/.claude/worktrees/workbench-pt-score-panel' (branch worktree-workbench-pt-score-panel) is fully merged into main.
```bash
git worktree remove C:/Users/cladi/Pendulastic/.claude/worktrees/workbench-pt-score-panel && git branch -d worktree-workbench-pt-score-panel
```

8. `[Safe to Delete]` Worktree 'C:/Users/cladi/Pendulastic/.claude/worktrees/workbench-raw-sensor-crosschecks' (branch worktree-workbench-raw-sensor-crosschecks) is fully merged into main.
```bash
git worktree remove C:/Users/cladi/Pendulastic/.claude/worktrees/workbench-raw-sensor-crosschecks && git branch -d worktree-workbench-raw-sensor-crosschecks
```

9. `[Safe to Delete]` Worktree 'C:/Users/cladi/Pendulastic/.claude/worktrees/workbench-viewer-style' (branch worktree-workbench-viewer-style) is fully merged into main.
```bash
git worktree remove C:/Users/cladi/Pendulastic/.claude/worktrees/workbench-viewer-style && git branch -d worktree-workbench-viewer-style
```

10. `[Safe to Delete]` Worktree 'C:/Users/cladi/Pendulastic/.worktrees/hotspot-setup-helper' (branch hotspot-setup-helper) is fully merged into main.
```bash
git worktree remove C:/Users/cladi/Pendulastic/.worktrees/hotspot-setup-helper && git branch -d hotspot-setup-helper
```

11. `[Safe to Delete]` Worktree 'C:/Users/cladi/Pendulastic/.worktrees/longitudinal-dashboard' (branch worktree-longitudinal-dashboard) is fully merged into main.
```bash
git worktree remove C:/Users/cladi/Pendulastic/.worktrees/longitudinal-dashboard && git branch -d worktree-longitudinal-dashboard
```

12. `[Safe to Delete]` Worktree 'C:/Users/cladi/Pendulastic/.worktrees/ms-vs-control-cohort' (branch worktree-ms-vs-control-cohort) is fully merged into main.
```bash
git worktree remove C:/Users/cladi/Pendulastic/.worktrees/ms-vs-control-cohort && git branch -d worktree-ms-vs-control-cohort
```

13. `[Needs Review]` '.context/' is untracked with no known pattern match - needs a human look.
```bash
git status -- .context/  # decide: add, gitignore, or delete
```

14. `[Needs Review]` '.superpowers/' is untracked with no known pattern match - needs a human look.
```bash
git status -- .superpowers/  # decide: add, gitignore, or delete
```

15. `[Needs Review]` 'ALGORITHM_LAUNCH_CONFIRMATION.md' is untracked with no known pattern match - needs a human look.
```bash
git status -- ALGORITHM_LAUNCH_CONFIRMATION.md  # decide: add, gitignore, or delete
```

16. `[Needs Review]` 'GOAL_ACHIEVEMENT_ASSESSMENT.md' is untracked with no known pattern match - needs a human look.
```bash
git status -- GOAL_ACHIEVEMENT_ASSESSMENT.md  # decide: add, gitignore, or delete
```

17. `[Needs Review]` 'IMPACT_MEASUREMENT_REPORT.md' is untracked with no known pattern match - needs a human look.
```bash
git status -- IMPACT_MEASUREMENT_REPORT.md  # decide: add, gitignore, or delete
```

18. `[Needs Review]` '_deprecated/' is untracked historical/vendored content - review before deleting.
```bash
rm -rf _deprecated/  # only after review
```

19. `[Needs Review]` 'calibration_coefficients.json' is untracked with no known pattern match - needs a human look.
```bash
git status -- calibration_coefficients.json  # decide: add, gitignore, or delete
```

20. `[Needs Review]` 'calibration_coefficients_lying_down.json' is untracked with no known pattern match - needs a human look.
```bash
git status -- calibration_coefficients_lying_down.json  # decide: add, gitignore, or delete
```

21. `[Needs Review]` 'calibration_coefficients_upright.json' is untracked with no known pattern match - needs a human look.
```bash
git status -- calibration_coefficients_upright.json  # decide: add, gitignore, or delete
```

22. `[Needs Review]` 'docs/superpowers/plans/2026-07-29-pendulastic-ux-fixes.md' is untracked with no known pattern match - needs a human look.
```bash
git status -- docs/superpowers/plans/2026-07-29-pendulastic-ux-fixes.md  # decide: add, gitignore, or delete
```

23. `[Needs Review]` 'docs/superpowers/plans/2026-08-07-mediapipe-patient-identity-tracking.md' is untracked with no known pattern match - needs a human look.
```bash
git status -- docs/superpowers/plans/2026-08-07-mediapipe-patient-identity-tracking.md  # decide: add, gitignore, or delete
```

24. `[Needs Review]` models\HigherHRNet-Human-Pose-Estimation-master\lib\fp16_utils\fp16_optimizer.py:506: unreachable code after 'raise' (100% confidence)
```bash
# review models\HigherHRNet-Human-Pose-Estimation-master\lib\fp16_utils\fp16_optimizer.py:506 before removing
```

25. `[Needs Review]` models\pose2sim-main\Pose2Sim\poseEstimation.py:791: unused import 'as_completed' (90% confidence)
```bash
# review models\pose2sim-main\Pose2Sim\poseEstimation.py:791 before removing
```

26. `[Needs Review]` models\PosePipeline\pose_pipeline\wrappers\fairmot.py:123: redundant if-condition (100% confidence)
```bash
# review models\PosePipeline\pose_pipeline\wrappers\fairmot.py:123 before removing
```

27. `[Needs Review]` models\PosePipeline\PosePipeline-main\pose_pipeline\wrappers\fairmot.py:123: redundant if-condition (100% confidence)
```bash
# review models\PosePipeline\PosePipeline-main\pose_pipeline\wrappers\fairmot.py:123 before removing
```

28. `[Needs Review]` models\SKEL-master\skel\utils.py:208: unreachable code after 'return' (100% confidence)
```bash
# review models\SKEL-master\skel\utils.py:208 before removing
```

29. `[Needs Review]` recalibrate_lying_down.py:203: unsatisfiable 'ternary' condition (100% confidence)
```bash
# review recalibrate_lying_down.py:203 before removing
```

30. `[Needs Review]` tests\test_pt_cohort_common.py:471: unused variable 'reg' (100% confidence)
```bash
# review tests\test_pt_cohort_common.py:471 before removing
```

31. `[Needs Review]` tests\test_pt_cohort_common.py:525: unused variable 'reg' (100% confidence)
```bash
# review tests\test_pt_cohort_common.py:525 before removing
```

32. `[Needs Review]` tests\test_pt_score.py:314: unused variable 'rec_dir_arg' (100% confidence)
```bash
# review tests\test_pt_score.py:314 before removing
```

33. `[Needs Review]` tests\test_pt_score.py:314: unused variable 'trial_arg' (100% confidence)
```bash
# review tests\test_pt_score.py:314 before removing
```

34. `[Needs Review]` tests\test_rmse_pipeline_common.py:687: unused variable 'mp_' (100% confidence)
```bash
# review tests\test_rmse_pipeline_common.py:687 before removing
```

35. `[Needs Review]` tests\test_rmse_pipeline_common.py:748: unused variable 'mp_' (100% confidence)
```bash
# review tests\test_rmse_pipeline_common.py:748 before removing
```

36. `[Needs Review]` tests\test_rmse_pipeline_common.py:759: unused variable 'mp_' (100% confidence)
```bash
# review tests\test_rmse_pipeline_common.py:759 before removing
```

37. `[Doc Drift Fix]` DEPLOYMENT_PLAN.md's status banner still reads "READY FOR PRODUCTION DEPLOYMENT" / "Approval Required: Yes" and frames `git merge worktree-imu-stillness-gyro-bias` as a future step, but commit 03a4a62 (the merge it describes) has been on main since 2026-08-05 and is already pushed to origin/main - the doc was flagged for this same drift in the 2026-08-07 report and is still unresolved.
```bash
# update DEPLOYMENT_PLAN.md's status banner and merge instructions to reflect that the deployment already happened (commit 03a4a62), or archive the doc
```

38. `[Doc Drift Fix]` DEPLOYMENT_PLAN.md still claims "docs/superpowers/plans/2026-08-04-imu-stillness-gyro-bias.md ... ~3500 lines total", but the file is actually 2146 lines - flagged in the 2026-08-07 report and still unresolved.
```bash
# correct the line-count figure in DEPLOYMENT_PLAN.md's Documentation section, or remove it
```

39. `[Doc Drift Fix]` DEPLOYMENT_PLAN.md's References section and ALGORITHM_LAUNCH_CONFIRMATION.md's "General Question" line both point at `.superpowers/sdd/2026-08-04-imu-stillness-gyro-bias/` (task-5-report.md, task-10-integration-report.md, FINAL-SUMMARY.md) - that directory still does not exist anywhere on disk (only same-named plan/spec docs exist under docs/superpowers/). Flagged in the 2026-08-07 report and still unresolved.
```bash
# either restore/commit the missing .superpowers/sdd/2026-08-04-imu-stillness-gyro-bias/ reports, or update the references in DEPLOYMENT_PLAN.md and ALGORITHM_LAUNCH_CONFIRMATION.md to point at files that exist
```

40. `[Doc Drift Fix]` README.md's Project Structure section shows a minimal tree (`data/`, `notebooks/`, `src/pendulastic/{video,pose,metrics,stats,utils}.py`, `tests/`, `requirements.txt`, `setup.py`, `README.md`) that no longer matches the repo root, which now holds 100+ additional top-level scripts, logs, and status docs (e.g. `pendulastic_imu_server.py`, `imu_calibration_tuner.py`, `evaluate_all_participants.py`, `analyze_accel_drift.py`, `DEPLOYMENT_PLAN.md`) plus an undocumented `src/pendulastic/hygiene/` subpackage.
```bash
# either regenerate README.md's Project Structure tree to reflect the current repo layout, or explicitly scope it as illustrative rather than literal
```

41. `[Doc Drift Fix]` README.md's Key Features section states the project runs "using standard smartphone or computer cameras" with "no wearable sensors ... required," but a substantial, actively-developed part of the codebase (`pendulastic_imu_server.py`, `imu_calibration_tuner.py`, plus the IMU accel-bias-correction work documented in DEPLOYMENT_PLAN.md, ALGORITHM_LAUNCH_CONFIRMATION.md, GOAL_ACHIEVEMENT_ASSESSMENT.md, and IMPACT_MEASUREMENT_REPORT.md) is IMU-sensor-based. README.md does not mention this second, sensor-based modality anywhere.
```bash
# update README.md's Overview/Key Features to describe the IMU-based pathway alongside the markerless-video one, or clarify that IMU work is a separate research track
```

## Low-confidence dead-code findings (not numbered - review only)

- models\openpose\3rdparty\pybind11\tests\test_tagbased_polymorphic.py:22: unused attribute 'sound' (60% confidence)
- models\PosePipeline\pose_pipeline\utils\jupyter.py:4: unused function 'play' (60% confidence)
- models\PosePipeline\PosePipeline-main\pose_pipeline\utils\jupyter.py:4: unused function 'play' (60% confidence)
- tests\test_imu_calibration_tuner.py:299: unused variable 't_contam' (60% confidence)
- tests\test_pendulastic_storage.py:12: unused function '_isolated_participants_dir' (60% confidence)
- tests\test_pendulastic_workbench.py:14: unused function '_isolated_participants_dir' (60% confidence)
