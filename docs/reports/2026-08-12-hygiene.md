# Hygiene Report - 2026-08-12

1. `[Needs Review]` Worktree 'C:/Users/cladi/Pendulastic/.claude/worktrees/mediapipe-hpe-preprocessing' (branch worktree-mediapipe-hpe-preprocessing) is locked (reason: claude session mediapipe-hpe-preprocessing (pid 23996)) - it is in active use; confirm with whoever locked it before removing.
```bash
git worktree list --porcelain  # locked worktree - do not remove without review
```

2. `[Needs Review]` 'docs/superpowers/plans/2026-08-11-mediapipe-hpe-preprocessing.md' is untracked with no known pattern match - needs a human look.
```bash
git status -- docs/superpowers/plans/2026-08-11-mediapipe-hpe-preprocessing.md  # decide: add, gitignore, or delete
```

3. `[Needs Review]` models\HigherHRNet-Human-Pose-Estimation-master\lib\fp16_utils\fp16_optimizer.py:506: unreachable code after 'raise' (100% confidence)
```bash
# review models\HigherHRNet-Human-Pose-Estimation-master\lib\fp16_utils\fp16_optimizer.py:506 before removing
```

4. `[Needs Review]` models\pose2sim-main\Pose2Sim\poseEstimation.py:791: unused import 'as_completed' (90% confidence)
```bash
# review models\pose2sim-main\Pose2Sim\poseEstimation.py:791 before removing
```

5. `[Needs Review]` models\PosePipeline\pose_pipeline\wrappers\fairmot.py:123: redundant if-condition (100% confidence)
```bash
# review models\PosePipeline\pose_pipeline\wrappers\fairmot.py:123 before removing
```

6. `[Needs Review]` models\PosePipeline\PosePipeline-main\pose_pipeline\wrappers\fairmot.py:123: redundant if-condition (100% confidence)
```bash
# review models\PosePipeline\PosePipeline-main\pose_pipeline\wrappers\fairmot.py:123 before removing
```

7. `[Needs Review]` models\SKEL-master\skel\utils.py:208: unreachable code after 'return' (100% confidence)
```bash
# review models\SKEL-master\skel\utils.py:208 before removing
```

8. `[Needs Review]` recalibrate_lying_down.py:203: unsatisfiable 'ternary' condition (100% confidence)
```bash
# review recalibrate_lying_down.py:203 before removing
```

9. `[Needs Review]` tests\test_pt_cohort_common.py:471: unused variable 'reg' (100% confidence)
```bash
# review tests\test_pt_cohort_common.py:471 before removing
```

10. `[Needs Review]` tests\test_pt_cohort_common.py:525: unused variable 'reg' (100% confidence)
```bash
# review tests\test_pt_cohort_common.py:525 before removing
```

11. `[Needs Review]` tests\test_pt_report_common.py:85: unused variable 'a_' (100% confidence)
```bash
# review tests\test_pt_report_common.py:85 before removing
```

12. `[Needs Review]` tests\test_pt_report_common.py:96: unused variable 'a_' (100% confidence)
```bash
# review tests\test_pt_report_common.py:96 before removing
```

13. `[Needs Review]` tests\test_pt_report_common.py:384: unused variable 'a_' (100% confidence)
```bash
# review tests\test_pt_report_common.py:384 before removing
```

14. `[Needs Review]` tests\test_pt_report_common.py:532: unused variable 'a_' (100% confidence)
```bash
# review tests\test_pt_report_common.py:532 before removing
```

15. `[Needs Review]` tests\test_pt_score.py:432: unused variable 'rec_dir_arg' (100% confidence)
```bash
# review tests\test_pt_score.py:432 before removing
```

16. `[Needs Review]` tests\test_pt_score.py:432: unused variable 'trial_arg' (100% confidence)
```bash
# review tests\test_pt_score.py:432 before removing
```

17. `[Needs Review]` tests\test_rmse_pipeline_common.py:687: unused variable 'mp_' (100% confidence)
```bash
# review tests\test_rmse_pipeline_common.py:687 before removing
```

18. `[Needs Review]` tests\test_rmse_pipeline_common.py:748: unused variable 'mp_' (100% confidence)
```bash
# review tests\test_rmse_pipeline_common.py:748 before removing
```

19. `[Needs Review]` tests\test_rmse_pipeline_common.py:759: unused variable 'mp_' (100% confidence)
```bash
# review tests\test_rmse_pipeline_common.py:759 before removing
```

20. `[Doc Drift Fix]` DEPLOYMENT_PLAN.md's "New Analysis & Test Files" section describes `integration_test_task10.py` as "200+ lines", but the file is actually 113 lines.
```bash
# correct the line-count figure for integration_test_task10.py in DEPLOYMENT_PLAN.md, or drop it
```

> Note: all `[Doc Drift Fix]` items flagged in the 2026-08-07/08-10/08-11 reports (DEPLOYMENT_PLAN.md's
> stale "READY FOR PRODUCTION" banner, the plan line-count mismatch, the missing `.superpowers/sdd/`
> references, README's literal-looking Project Structure tree, README's missing IMU-pathway mention,
> and ALGORITHM_LAUNCH_CONFIRMATION.md's stale line-number citations) have since been resolved and were
> re-verified clean during this run - not repeated here. Core entry points (`pendulastic_app.py`,
> `master_app.py`, `pendulastic_imu_server.py`, `imu_calibration_tuner.py`), the README Quick Start
> API (`pendulastic.video.load_video`/`preprocess_frames`, `pendulastic.pose.extract_landmarks`,
> `pendulastic.metrics.compute_knee_angles`/`extract_pendulum_metrics`), and the `zero()`/`accel_bias`/
> `calibrate_accel_bias()` claims in ALGORITHM_LAUNCH_CONFIRMATION.md and DEPLOYMENT_PLAN.md were all
> checked against the current code and match.

=== NEXT_ITEM_NUMBER: 21 ===
