# Hygiene Report - 2026-08-18

Phase 2 INCOMPLETE: vulture timed out after 300s

1. `[Needs Review]` Worktree 'C:/Users/cladi/Pendulastic/.claude/worktrees/trial-exclusion-ui' (branch worktree-trial-exclusion-ui) is locked (reason: claude session trial-exclusion-ui (pid 1388)) - it is in active use; confirm with whoever locked it before removing.
```bash
git worktree list --porcelain  # locked worktree - do not remove without review
```

2. `[Needs Review]` 'compare_2d_vs_world_angle.py' is untracked with no known pattern match - needs a human look.
```bash
git status -- compare_2d_vs_world_angle.py  # decide: add, gitignore, or delete
```

3. `[Needs Review]` 'docs/reports/2026-08-17-imu-methodology-comparison.md' is untracked with no known pattern match - needs a human look.
```bash
git status -- docs/reports/2026-08-17-imu-methodology-comparison.md  # decide: add, gitignore, or delete
```

4. `[Needs Review]` 'docs/superpowers/plans/2026-08-07-trial-exclusion-ui.md' is untracked with no known pattern match - needs a human look.
```bash
git status -- docs/superpowers/plans/2026-08-07-trial-exclusion-ui.md  # decide: add, gitignore, or delete
```

5. `[Needs Review]` 'evaluate_ft_ratio_sweep.py' is untracked with no known pattern match - needs a human look.
```bash
git status -- evaluate_ft_ratio_sweep.py  # decide: add, gitignore, or delete
```

6. `[Needs Review]` 'evaluate_mag_toggle.py' is untracked with no known pattern match - needs a human look.
```bash
git status -- evaluate_mag_toggle.py  # decide: add, gitignore, or delete
```

7. `[Needs Review]` 'evaluate_mediapipe_vs_imu_rmse.py' is untracked with no known pattern match - needs a human look.
```bash
git status -- evaluate_mediapipe_vs_imu_rmse.py  # decide: add, gitignore, or delete
```

8. `[Needs Review]` 'evaluate_ockendon_methodology.py' is untracked with no known pattern match - needs a human look.
```bash
git status -- evaluate_ockendon_methodology.py  # decide: add, gitignore, or delete
```

9. `[Needs Review]` 'evaluate_tuning_grid_methodology.py' is untracked with no known pattern match - needs a human look.
```bash
git status -- evaluate_tuning_grid_methodology.py  # decide: add, gitignore, or delete
```

10. `[Needs Review]` 'ft_ratio_sweep_output.txt' is untracked with no known pattern match - needs a human look.
```bash
git status -- ft_ratio_sweep_output.txt  # decide: add, gitignore, or delete
```

11. `[Needs Review]` 'ft_ratio_sweep_wide_output.txt' is untracked with no known pattern match - needs a human look.
```bash
git status -- ft_ratio_sweep_wide_output.txt  # decide: add, gitignore, or delete
```

12. `[Needs Review]` 'full_grid_sweep_output.txt' is untracked with no known pattern match - needs a human look.
```bash
git status -- full_grid_sweep_output.txt  # decide: add, gitignore, or delete
```

13. `[Needs Review]` 'mag_toggle_output.txt' is untracked with no known pattern match - needs a human look.
```bash
git status -- mag_toggle_output.txt  # decide: add, gitignore, or delete
```

14. `[Needs Review]` 'mediapipe_vs_imu_output.txt' is untracked with no known pattern match - needs a human look.
```bash
git status -- mediapipe_vs_imu_output.txt  # decide: add, gitignore, or delete
```

15. `[Needs Review]` 'pendulastic-developer-spec.md' is untracked with no known pattern match - needs a human look.
```bash
git status -- pendulastic-developer-spec.md  # decide: add, gitignore, or delete
```

16. `[Needs Review]` 'reprocess_p16_rotate90.py' is untracked with no known pattern match - needs a human look.
```bash
git status -- reprocess_p16_rotate90.py  # decide: add, gitignore, or delete
```

17. `[Needs Review]` 'sweep_p16_mask_assessor.py' is untracked with no known pattern match - needs a human look.
```bash
git status -- sweep_p16_mask_assessor.py  # decide: add, gitignore, or delete
```

18. `[Needs Review]` 'sweep_p16_model_variant.py' is untracked with no known pattern match - needs a human look.
```bash
git status -- sweep_p16_model_variant.py  # decide: add, gitignore, or delete
```

19. `[Needs Review]` 'sweep_p16_preprocessing.py' is untracked with no known pattern match - needs a human look.
```bash
git status -- sweep_p16_preprocessing.py  # decide: add, gitignore, or delete
```

20. `[Needs Review]` 'sweep_resume_log.txt' is untracked with no known pattern match - needs a human look.
```bash
git status -- sweep_resume_log.txt  # decide: add, gitignore, or delete
```

21. `[Needs Review]` 'sweep_resume_log10.txt' is untracked with no known pattern match - needs a human look.
```bash
git status -- sweep_resume_log10.txt  # decide: add, gitignore, or delete
```

22. `[Needs Review]` 'sweep_resume_log11.txt' is untracked with no known pattern match - needs a human look.
```bash
git status -- sweep_resume_log11.txt  # decide: add, gitignore, or delete
```

23. `[Needs Review]` 'sweep_resume_log12.txt' is untracked with no known pattern match - needs a human look.
```bash
git status -- sweep_resume_log12.txt  # decide: add, gitignore, or delete
```

24. `[Needs Review]` 'sweep_resume_log13.txt' is untracked with no known pattern match - needs a human look.
```bash
git status -- sweep_resume_log13.txt  # decide: add, gitignore, or delete
```

25. `[Needs Review]` 'sweep_resume_log2.txt' is untracked with no known pattern match - needs a human look.
```bash
git status -- sweep_resume_log2.txt  # decide: add, gitignore, or delete
```

26. `[Needs Review]` 'sweep_resume_log3.txt' is untracked with no known pattern match - needs a human look.
```bash
git status -- sweep_resume_log3.txt  # decide: add, gitignore, or delete
```

27. `[Needs Review]` 'sweep_resume_log4.txt' is untracked with no known pattern match - needs a human look.
```bash
git status -- sweep_resume_log4.txt  # decide: add, gitignore, or delete
```

28. `[Needs Review]` 'sweep_resume_log5.txt' is untracked with no known pattern match - needs a human look.
```bash
git status -- sweep_resume_log5.txt  # decide: add, gitignore, or delete
```

29. `[Needs Review]` 'sweep_resume_log6.txt' is untracked with no known pattern match - needs a human look.
```bash
git status -- sweep_resume_log6.txt  # decide: add, gitignore, or delete
```

30. `[Needs Review]` 'sweep_resume_log7.txt' is untracked with no known pattern match - needs a human look.
```bash
git status -- sweep_resume_log7.txt  # decide: add, gitignore, or delete
```

31. `[Needs Review]` 'sweep_resume_log8.txt' is untracked with no known pattern match - needs a human look.
```bash
git status -- sweep_resume_log8.txt  # decide: add, gitignore, or delete
```

32. `[Needs Review]` 'sweep_resume_log9.txt' is untracked with no known pattern match - needs a human look.
```bash
git status -- sweep_resume_log9.txt  # decide: add, gitignore, or delete
```

33. `[Doc Drift Fix]` `DEPLOYMENT_PLAN.md` and `IMPACT_MEASUREMENT_REPORT.md` both cite `integration_test_task10.py` as validation evidence ("6/6 PASS", "Integration: FLAWLESS"). Running it now against current code fails at collection: `_IMUDevice.accel_bias` comes out as `[0.0019, -0.00095, 0.1906]` vs. the test's hardcoded expectation of `[0.1, -0.05, 0.19]` (x/y off by ~50x). Separately, the filename doesn't match pytest's default discovery globs (`test_*.py` / `*_test.py`), so it silently doesn't run under a bare `pytest` invocation either — the "6/6 PASS" claim has not been exercised by CI since.
```bash
.venv/Scripts/python.exe -m pytest integration_test_task10.py -v  # reproduce the failure
```

34. `[Doc Drift Fix]` `GOAL_ACHIEVEMENT_ASSESSMENT.md`, `ALGORITHM_LAUNCH_CONFIRMATION.md`, and `IMPACT_MEASUREMENT_REPORT.md` (all dated 2026-08-05) frame the "<5 degrees RMSE" goal as "ON TRACK (70% Complete)" / "HIGH confidence" / "Expected outcome: VALIDATED", pending "production monitoring data in 7-14 days" to confirm. That window closed around 2026-08-16-19. `docs/reports/2026-08-17-imu-methodology-comparison.md` (currently untracked, see item 3) now reports the real measured baseline against OptiTrack ground truth across 53 trials: mean RMSE 16.8°, median 14.0°, only 1/53 trials under the 5° goal — the monitoring answer has arrived and contradicts the three docs' optimistic framing, but none of them have been updated to reflect it.
```bash
diff <(grep -m1 'Goal Status' GOAL_ACHIEVEMENT_ASSESSMENT.md) <(grep -m1 'Baseline' docs/reports/2026-08-17-imu-methodology-comparison.md)  # compare stale goal framing vs measured baseline
```

=== NEXT_ITEM_NUMBER: 35 ===
